import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.com.webento.note',
  appName: 'Smile Note',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
